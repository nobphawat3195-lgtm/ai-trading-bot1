# CURRENT_EA_ANALYSIS.md

วิเคราะห์ `MQL5/Experts/XAU_StraddleReverse_v4_40.mq5` (source v4.41, ล่าสุดที่มีจริงใน repo)
XAUUSD, ออกแบบไว้สำหรับ M1

## Entry

- **Straddle เข้าคู่**: เมื่อไม่มีไม้และไม่มีออเดอร์ค้าง วาง `BuyStop` ที่ `ask + straddle_dist`
  และ `SellStop` ที่ `bid − straddle_dist` พร้อมกัน (`PlaceStraddle()`, บรรทัด 504-511)
- เข้าได้เฉพาะเมื่อผ่านเงื่อนไขทั้งหมด: อยู่ใน session window, ไม่อยู่ใน cooldown, ยังไม่โดน
  SL ครบโควตาวันนี้, สเปรด ≤ `InpMaxSpread`, และผ่าน **ATR gate** (ATR เร็ว(14)/ATR ช้า(60)
  ของแท่ง M1 ≤ `atr_expand_max` — กรองเอาเฉพาะช่วงที่ความผันผวน "กำลังหด" ก่อนเข้า)
- เมื่อฝั่งใดฝั่งหนึ่งโดนชน อีกฝั่งที่เหลือถูกลบทิ้งทันที (ดู `pend.clear()` / logic ลบ
  ออเดอร์ฝั่งเดียวกับไม้ที่ถืออยู่ บรรทัด 1242-1255)

## Exit

สามทาง:
1. **Stop Loss** — ระยะคงที่ (`$` ต่อออนซ์ หรือจุด) ถูกโบรกบังคับขั้นต่ำผ่าน `EffSLPts()`
2. **Reverse (กลับไม้)** — เมื่อกำไรถึง `InpReverseStartPts` วางออเดอร์ดักฝั่งตรงข้าม
   (`gap` ถูกบีบให้ ≤ 80% ของ SL จริงเสมอ ผ่าน `ResolveReverseGapPts()`) แล้วไล่ตามราคาเข้า
   หาเรื่อยๆ (ไม่ถอยออก) — ถ้าราคาย้อนกลับมาชนกับดัก ปิดไม้เดิมแล้วเปิดไม้ฝั่งตรงข้ามทันที
3. **หมดช่วงเวลา** — ถ้าเปิด `InpCloseAllAtSessionEnd` จะปิดทั้งหมดตอนหมด session; ถ้าปิด
   option นี้ (ค่าเริ่มต้นในพรีเซ็ตคือ `false`) ไม้ที่ถืออยู่จะข้ามคืนต่อไปได้

ไม่มี fixed TP เป็นค่าหลัก (`InpTakeProfit=0.0` ใน preset ที่ตรวจ) — ปล่อยให้ trailing/reverse
จัดการกำไรแทน

## SL / TP mechanics

- หน่วยเลือกได้ (USD/ออนซ์ หรือจุด) แปลงผ่าน `ToPts()`/`Pt()`
- SL จริงที่ใช้เทรด (`EffSLPts()`) ถูกถ่างขึ้นเสมอถ้าค่าที่กรอกแคบกว่า `StopsLevel`/
  `FreezeLevel` ของโบรก — และ **CalcLot() ใช้ SL จริงนี้คำนวณขนาดไม้ ไม่ใช่ค่าที่กรอก**
  (แก้ไว้ตั้งแต่ v4.40) จุดนี้ทำถูกแล้ว แต่เป็นหลักฐานว่า SL/lot สัมพันธ์กับสเปกโบรกมาก
  — เปลี่ยนโบรกแล้วพฤติกรรมจริงเปลี่ยนได้โดยไม่รู้ตัวถ้าไม่ตรวจ `MinStopPts()` ใหม่ทุกครั้ง

## Breakeven / Trailing

- Breakeven: กำไร ≥ `InpBEStart` → เลื่อน SL มาที่ open + `InpBELock`
- Trailing: กำไร ≥ `InpTrailStart` → SL ไล่ตามห่าง `InpTrailDist` (มีลำดับความสำคัญเหนือ BE)
- ทั้งสองเป็น threshold คงที่หน่วย $/ออนซ์ ไม่ผูกกับความผันผวนขณะนั้น (ไม่ใช่ ATR-based
  trailing) — ระยะเดียวกันใช้ทั้งช่วงตลาดนิ่งและช่วงตลาดแรง

## Indicators / Filters

- มีตัวเดียวที่ใช้ตัดสินใจเข้า: **ATR fast(14)/ATR slow(60) ratio บนแท่ง M1** (volatility
  contraction filter) — ไม่มี indicator ทิศทาง (ไม่มี MA, RSI, momentum ใดๆ)
- Spread filter (`InpMaxSpread`) เป็นตัวกรองความปลอดภัยด้านต้นทุน ไม่ใช่ signal
- ไม่มีตัวกรองข่าว/event, ไม่มีตัวกรอง trend/regime แยกจาก ATR ratio

## Session

- 3 หน้าต่างเวลาที่ตั้งค่าได้ (`S1/S2/S3`) — ใน source v4.41 ตีความเป็น **เวลาไทย**
  (`ThaiTimeStr()`, `SessionOK()` แปลง `dt.hour/min` เป็นเวลาไทยก่อนเทียบ) ขณะที่เอกสาร
  v4.60/v5.00 ใน `docs/EXNESS_vs_VTMARKETS.md` เขียนว่าอินพุตควรตีความเป็น **UTC** —
  ยืนยันไม่ได้ว่า source ปัจจุบันตรงกับพฤติกรรมล่าสุดที่ตั้งใจ (ดูหัวข้อ Overfitting/
  Repainting ด้านล่าง)
- ค่า preset (`02_backtest_verify.ini`): S1 07:00-08:30, S3 02:00-03:30 (S2 ปิด) —
  ตามเอกสารคือพยายามจับช่วง London open / NY session overlap แต่ **ไม่มีเหตุผลเชิงกลไก
  ตลาด (liquidity, การเปิดตลาดจริง) ผูกไว้ในโค้ด** เป็นแค่ตัวเลขคงที่ที่น่าจะมาจากการ optimize
  บนข้อมูลย้อนหลัง

## Risk Management / Position Management

- Lot: auto (% risk ของ balance) หรือ fixed, cap ที่ `InpMaxLot`
- คูลดาวน์หลัง SL (`InpCooldownSec`, ปิด pending ระหว่างคูลดาวน์ได้)
- Daily breaker: `InpMaxSLPerDay` (0 = ปิด)
- **ถือได้ครั้งละ 1 ไม้เท่านั้น** (2 ไม้ชั่วขณะระหว่างเฟรมที่กำลังกลับไม้ แล้วปิดไม้เก่าทันที)
- Hedging account เท่านั้น (ตรวจใน `OnInit`, ถ้า netting → `INIT_FAILED`)

## Market Regime ที่ EA พึ่งพา

กลไกคือ **breakout จากช่วงบีบตัว (compression) ในหน้าต่างเวลาคงที่** แล้วใช้ stop-and-reverse
เป็นตัวจัดการทิศทางที่ผิด กลยุทธ์นี้เป็นแนว **long-gamma / short-theta** — กำไรมาจากการที่ราคา
วิ่งแรงพอจะครอบคลุมสเปรด+คอมมิชชัน+ระยะคร่อม 2 ฝั่ง หลังจากนั้น reverse ช่วยลด downside
เวลาเดาทิศทางผิดในจังหวะแรก แต่ไม่ได้ป้องกัน whipsaw (ราคาแกว่งไปมาในกรอบแคบซ้ำๆ) ซึ่งเป็น
สภาพตลาดที่ทำร้ายกลยุทธ์นี้มากที่สุด

## Trade Frequency

จำกัดด้วย session window (รวมสูงสุด ~2.5-3 ชม./วันจาก 3 ช่วง), ถือได้ 1 ไม้ต่อครั้ง,
cooldown หลัง SL — ความถี่ต่ำถึงปานกลาง (ระดับหลักสิบ-ร้อยไม้/เดือน ไม่ใช่ high-frequency)

## Potential Edge (ตามที่ผู้ออกแบบตั้งใจ)

Session-driven volatility expansion: ช่วงเปิดตลาดสำคัญ (London/NY open ตามเจตนาของเอกสาร)
มักมี breakout หลังช่วงบีบตัว — straddle+reverse พยายามจับทิศทางที่แท้จริงโดยไม่ต้องทำนาย
ล่วงหน้าว่าจะขึ้นหรือลง

## Weakness

1. **ต้นทุนกินขอบตรงๆ**: กำไรเฉลี่ย/ไม้ตามเอกสาร ~$0.49 ขณะที่สเปรด+คอมมิชชันอยู่ในระดับ
   ใกล้เคียงกัน (`docs/EXNESS_vs_VTMARKETS.md` หัวข้อ 4 และ 8) — ขอบบางมาก อ่อนไหวสูงต่อ
   ต้นทุนที่เปลี่ยนแม้เพียงเล็กน้อย
2. **Whipsaw risk**: ไม่มีกลไกแยกแยะ "breakout จริง" กับ "false breakout ในกรอบแคบ" นอกจาก
   ATR ratio ตัวเดียว ซึ่งเป็นตัวชี้วัดความผันผวนหดตัว ไม่ใช่ตัวชี้วัดว่าจะ breakout ทิศไหน
3. **Session window ไม่มีเหตุผลเชิงกลไกตลาด** — เป็นตัวเลขนาฬิกาคงที่ 3 ช่วง ไม่ผูกกับ
   liquidity/news event จริง มีความเสี่ยงสูงที่เป็นผลจากการ optimize บนข้อมูลย้อนหลัง
   (data-mined window) มากกว่าเหตุผลเชิงโครงสร้างตลาด
4. **Reverse gap ผูกกับ SL เสมอ (≤80%)** — ดีในแง่ป้องกัน gap ไกลกว่า SL แต่ผลคือกับดักกลับไม้
   ทำงานไวมาก การกลับไม้เกิดถี่ในสภาพตลาด sideways ที่แกว่งในกรอบพอๆ กับ gap — อาจสร้าง
   "กลับไม้ไปมา" ซ้ำหลายรอบต่อวันที่ไม่ได้ตั้งใจ

## Overfitting Risk — **สูง**

- เอกสาร `docs/EXNESS_vs_VTMARKETS.md` (หัวข้อ 1) ยืนยันเองว่า backtest 10 เดือนที่เคยอ้าง
  ผล +$599 นั้น **แท้จริงทดสอบ 2 หน้าต่างเวลา UTC คนละช่วงปนกัน** (เพราะ DST ของโบรกเดิม)
  โดยที่ผู้พัฒนาไม่รู้ตัวตอนแรก — แปลว่าตัวเลขผลตอบแทนในอดีตที่เคยมีนั้น **เชื่อถือไม่ได้
  จนกว่าจะแยกวิเคราะห์ใหม่เป็นรายหน้าต่างจริง**
- `03_optimize.ini` เปิด optimize พร้อมกันถึง 6 พารามิเตอร์ที่สัมพันธ์กัน
  (`InpStraddleDist`, `InpStopLoss`, `InpReverseStartPts`, `InpReverseGap`,
  `InpCooldownSec`, `InpS2_On`) ด้วย slow-complete algorithm บนช่วงข้อมูลเดียว (2026.06-08,
  3 เดือน) — ความเสี่ยง curve-fit สูงเมื่อไม่มีขั้นตอน OOS/walk-forward บังคับในเวิร์กโฟลว์
- Session window 3 ช่วงที่ตั้งไว้ (07:00-08:30, 19:00-20:30, 02:00-03:30 Thai/UTC ตามที่
  ตีความ) มีลักษณะเป็นค่าที่ "หาเจอแล้วใช้" มากกว่ามีที่มาจากกลไกตลาดที่อธิบายได้ล่วงหน้า

## Look-ahead Risk

- MT5 `Model=1` ("1 Minute OHLC") ในเทสเตอร์ตามที่ `02_backtest_verify.ini` ตั้งไว้ **มี
  ความละเอียดต่ำกว่า every-tick** — ไม่เห็น path ราคาในแท่งเดียวกัน อาจทำให้ backtest
  optimistic กว่าตลาดจริง โดยเฉพาะกับกลยุทธ์ที่ผูกกับ intrabar micro-structure (straddle
  ที่วางออเดอร์ห่างราคาปัจจุบันแค่ $0.50) — นี่คือความเสี่ยงระดับ **testing methodology**
  ไม่ใช่บั๊กในโค้ด EA เอง
- ตัวโค้ด EA เองไม่มี look-ahead ในความหมาย "อ่านข้อมูลอนาคต" — ทุก signal คำนวณจากราคา ณ
  เวลาปัจจุบันหรือแท่งที่ปิดแล้วเท่านั้น (ATR gate ใช้แท่ง M1 ที่ยุบเสร็จแล้ว)

## Repainting Risk

- ไม่มี indicator ที่ repaint (ATR gate คำนวณจากแท่งปิดแล้ว, ไม่มีการวาดสัญญาณย้อนหลัง)
- จุดที่ใกล้เคียงกับความเสี่ยงนี้คือ **`ServerGMTOffset()` ที่คืนค่าไม่เท่ากันระหว่างโหมด
  live กับโหมด tester/optimization โดยตั้งใจ** (บรรทัด 283-298) — เป็นการป้องกัน
  `TimeGMT()` ที่เชื่อถือไม่ได้ใน Strategy Tester ไม่ใช่บั๊ก แต่เป็นจุดที่ต้องระวังเวลา
  เทียบผล backtest กับพฤติกรรม live เพราะ path การคำนวณต่างกันจริง
