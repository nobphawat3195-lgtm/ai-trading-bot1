# FORBIDDEN_LOGIC.md

Logic ของ `XAU_StraddleReverse_v4_40.mq5` ที่ **ห้ามใช้เป็นแกนหลัก (core mechanism)**
ของ hypothesis ใหม่ในโปรเจกต์นี้ อ้างอิงจาก `CURRENT_EA_ANALYSIS.md`

เป้าหมาย: บังคับให้ Edge Discovery ค้นหา **กลไกตลาดที่ต่างจากเดิมจริง** ไม่ใช่แค่ reparameterize
EA เดิม เอาไปใส่ preset ใหม่แล้วเรียกว่า hypothesis ใหม่

## ห้าม 1 — Straddle (วางออเดอร์ 2 ฝั่งรอบราคาปัจจุบันพร้อมกัน) เป็น entry หลัก

เหตุผล: เป็นการเดิมพันว่า "จะ breakout แต่ไม่รู้ทิศทาง" — ไม่ใช่ signal ที่มาจากการอ่าน
พฤติกรรมตลาด (ไม่มีสมมติฐานว่าทำไมราคาจะไปทางไหน) hypothesis ใหม่ต้องมีเหตุผลเชิงกลไกว่า
"ทำไมทิศทางนี้" ไม่ใช่แค่ "รอราคาวิ่งแล้วเก็บฝั่งที่วิ่ง"

## ห้าม 2 — Stop-and-Reverse (พลิกฝั่งอัตโนมัติเมื่อ SL/กับดักโดนชน)

เหตุผลเดียวกับข้อ 1 — เป็นกลไกจัดการความไม่รู้ทิศทาง ไม่ใช่ edge เชิงพฤติกรรมตลาด
hypothesis ใหม่ต้องมี exit ที่ผูกกับเงื่อนไขตลาดจริง (โครงสร้างเปลี่ยน, สภาพคล่องหมด,
ถึงเป้าตามกลไก) ไม่ใช่ "ผิดทางแล้วพลิกกลับอัตโนมัติ"

## ห้าม 3 — ATR fast(14)/slow(60) contraction ratio เป็นตัวกรองหลัก (หรือตัวกรองเดียว)

เหตุผล: เป็นตัวชี้วัด "ความผันผวนกำลังหด" ทั่วไป ไม่เจาะจงกลไกตลาดใดๆ ใช้ร่วมเป็น
context filter รองได้ (เช่นในหมวด Volatility Regime) แต่ห้ามเป็นแกนสมมติฐานหลักซ้ำของเดิม

## ห้าม 4 — หน้าต่างเวลาคงที่แบบ curve-fit (S1/S2/S3 ตามนาฬิกา ไม่มีเหตุผลเชิงกลไก)

เหตุผล: ตามที่ระบุใน `CURRENT_EA_ANALYSIS.md` หัวข้อ Overfitting ช่วงเวลา 07:00-08:30,
19:00-20:30, 02:00-03:30 (Thai/UTC ตามที่ตีความ) เป็นค่าที่ "หาเจอจากการ optimize" มากกว่า
มีที่มาจากกลไกตลาดที่อธิบายได้ล่วงหน้า hypothesis ใหม่ในหมวด Session (E) ต้องอ้างอิงเหตุผล
เชิงโครงสร้างตลาดจริง (session open ของตลาดหลัก, overlap ของ liquidity provider,
เวลาประกาศข่าวมาตรฐาน) ไม่ใช่ตัวเลขที่ได้จากการกวาด parameter บนข้อมูลเก่า

## ห้าม 5 — Cooldown-after-SL / Max-SL-per-day เป็น "แหล่งของ edge"

เหตุผล: เป็นกลไกจำกัดความเสี่ยง (risk control) ไม่ใช่แหล่งที่มาของ statistical edge — ใช้
เป็นส่วนหนึ่งของ position management ของ candidate ใหม่ได้ตามปกติ แต่ห้ามอ้างว่านี่คือ
"เหตุผลที่ strategy ทำกำไร"

## ห้าม 6 — Breakeven/Trailing ด้วย threshold คงที่หน่วย $ ไม่ผูกความผันผวน

เหตุผล: ระยะเดียวกันใช้ทั้งตลาดนิ่งและตลาดแรง ไม่ปรับตามสภาพตลาดขณะนั้น — เป็นข้อจำกัดของ
เดิมที่ไม่ควรลอกมาใช้ตรงๆ โดยไม่คิดใหม่ ถ้า candidate ใหม่ต้องการ trailing ต้องให้เหตุผลว่า
ทำไมระยะนั้นเหมาะกับกลไกที่กำลังทดสอบ (เช่นผูกกับ ATR ของช่วงเวลานั้น)

---

## สิ่งที่อนุญาตให้ใช้ร่วมได้ (ไม่ถือเป็น "แกนหลักซ้ำเดิม")

- แนวคิดเรื่อง hedging account / position sizing ตาม % risk — เป็นโครงสร้างการเงินทั่วไป
  ไม่ใช่ edge
- Spread filter เป็นตัวกรองต้นทุน (safety filter) — ใช้ต่อได้ในทุก candidate
- โครงสร้างโค้ด MQL5 (การจัดการ error, normalize price, min-stop handling ของโบรก) —
  เป็น infrastructure ไม่ใช่ trading logic
- ATR ใช้เป็น **input ประกอบ** ของหมวด D (Volatility) ได้ ตราบใดที่ไม่ใช่ ratio
  14/60 ≤ threshold แบบเดียวกับเดิมทุกประการ และมีสมมติฐานกลไกตลาดใหม่ประกอบ

**กติกาการตรวจสอบ**: ทุก hypothesis ใน `research/hypotheses/` ต้องตอบได้ว่าแตกต่างจาก
Forbidden Logic ข้อ 1-4 อย่างไร ก่อนเข้าสู่ขั้น Statistical Discovery
